{#
/**
 * @file
 * Default theme implementation for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 *
 * @ingroup themeable
 */
#}
<input class="border border-1 border-[#bbb] 
px-[22px] py-[8px] mb-[40px]
rounded-[5px] text-[#279e95] text-center
cursor-pointer hover:bg-[#279e95] hover:text-[#fff]
"
{{ attributes }} />{{ children }}
