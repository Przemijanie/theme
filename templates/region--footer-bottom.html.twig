{#
/**
 * @file
 * Default theme implementation to dis§play a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 *
 * @ingroup themeable
 */
#}
{% if content %}
  <div {{ attributes }} class="bg-primary">
    <div class="mx-auto font-light text-white font-['Roboto'] text-[15px] lg:container px-[15px]">
      <div class="flex justify-center flex-wrap items-center md:justify-between flex-col sm:flex-row leading-5 gap-2 pt-2 sm:pt-0 text-center ">
        {{ content }}
      </div>
    </div>
  </div>
{% endif %}
